const connect = require('./connect');
const models = require('./models');
const rights = require('./rights');
const actions = require('./actions');
const io = require('./io');

module.exports = {
  connect, models, rights, actions, io
};
